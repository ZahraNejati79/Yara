import LoginForm from "../Components/LoginForm";
import Layout from "../Layout/Layout";

const LogInPage = () => {
  return (
    <Layout>
      <LoginForm />
    </Layout>
  );
};

export default LogInPage;
